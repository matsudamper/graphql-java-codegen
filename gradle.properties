# This fork publishes to GitHub Packages of matsudamper/graphql-java-codegen.
# Credentials are taken from the GITHUB_ACTOR / GITHUB_TOKEN environment variables
# (see the `publishing.repositories` block in build.gradle).
